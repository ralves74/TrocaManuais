@model IEnumerable<TrocaManuais.Web.Models.AnosEscolares>

@{
    ViewBag.Title = "Index";

}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ano)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Escaloes.Escalao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ano)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Escaloes.Escalao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idAEscola }) |
            @Html.ActionLink("Details", "Details", new { id=item.idAEscola }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idAEscola })
        </td>
    </tr>
}

</table>
